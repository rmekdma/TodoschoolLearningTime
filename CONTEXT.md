# TodoSchool Learning Time

토도스쿨(한글, 수학, 영어)의 일일 학습 시간 및 과목별 목표 달성 현황을 조회하는 안드로이드 앱 컨텍스트.

## Language

**조회 일자 (Learning Date)**:
학습 시간과 활동 내역을 조회하는 대상 날짜. 상단 네비게이션(`<`, `M월 d일 (E)`, `>`)을 통해 변경할 수 있으며 기본값은 오늘이다. 오늘인 경우 `>` 버튼은 `INVISIBLE` 처리되고, 오늘이 아닌 과거 날짜 조회 시에는 아래로 당겨 새로고침(SwipeRefresh)이 비활성화된다. DatePicker 좌측의 '오늘' 버튼으로 즉시 오늘 날짜 조회가 가능하다.
_Avoid_: 검색일, 선택일

**아이 (Child)**:
토도스쿨 계정에 등록된 학습자 프로필. 아이 이름으로 식별 및 집계된다.
_Avoid_: 사용자, 학생, 유저

**과목 (Subject)**:
학습 대상 영역으로 한글, 수학, 영어가 해당된다. 아이가 특정 과목을 구독/등록하지 않은 경우 해당 과목은 표시 및 설정 창에서 완전히 제외된다.
_Avoid_: 코스, 카테고리

**활동 수 (Activity Count)**:
해당 조회 일자에 아이가 실제로 완료한 학습 활동의 개수. 한글/영어는 `contentsHistory.dailyCourse`, 수학은 `contentsHistory.daily` 리스트의 길이를 의미한다.
_Avoid_: 학습수, 코스수, 완료수

**목표 개수 (Target Count)**:
과목 완료(✔)를 판정하기 위해 하루 동안 요구되는 활동 수. 아이별/과목별로 개별 설정 가능하며(기본값: 한글 6, 수학 5, 영어 8), 0 이상의 정수를 갖는다. 0으로 설정 시 항상 완료로 취급된다. 빈 값은 저장이 불가하며 placeholder로 기본값을 제공한다.
_Avoid_: 기준 개수, 목표치, 완수 기준

**과목 완료 현황 (Subject Status)**:
각 과목의 활동 수와 목표 개수를 항상 숫자로 나타낸 표기. 완료 시 `✔ 과목명 (활동수/목표개수)`, 미완료 시 `과목명 (활동수/목표개수)`로 표현된다.
_Avoid_: 클리어, 달성률
